version: '3.6'

services:
  <%= name %>-hobo:
    build: .
    image: <%= name %>:latest
    hostname: 'hobo'
    ports:
      - "8888:8888"
    <% if (flagTypeFile) { %>
    volumes:
      - ./<%= uuid %>.gn:/goldnugget/<%= uuid %>.gn
    <% } %>
    <% if (flagTypeEnv) { %>
    env_file: <%= uuid %>.env
    <% } %>
    <% if (!goldnugget) { %>
    # Remove the volumes and env_file properties if goldnugget is not selected
     volumes:
      - ./root/tinyproxy:/etc/tinyproxy
    <% } %>


