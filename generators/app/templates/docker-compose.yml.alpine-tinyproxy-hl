version: '3.6'

services:
  <%= name %>-hobo:
    build: .
    image: <%= name %>:latest
    hostname: 'hobo'
    ports:
      - "8888:8888"
      
<% if (flagType === 'env' && goldnugget) { %>
    env_file: <%= uuid %>.env
<% } else if (flagType === 'file' && goldnugget) { %>
    volumes:
      - ./<%= uuid %>.gn:/goldnugget/<%= uuid %>.gn
<% } else if (!goldnugget) { %>
    volumes:
      - ./root/tinyproxy:/etc/tinyproxy
<% } %>

