version: '3.6'

services:
  <%= name %>-hobo:
    build: .
    image: <%= name %>:latest
    hostname: 'hobo'
    ports:
      - "7681:7681"
    <% if (flagTypeFile) { %>
    volumes:
      - ./<%= uuid %>.gn:/goldnugget/<%= uuid %>.gn
    <% } %>
    <% if (flagTypeEnv) { %>
    env_file: <%= uuid %>.env
    <% } %>
    <% if (!goldnugget) { %>
    # Remove the volumes and env_file properties if goldnugget is not selected
    <% } %>
