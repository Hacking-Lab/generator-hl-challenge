version: '3.6'

services:
  <%= name %>-hobo:
    build: .
    image: <%= name %>:latest
    hostname: 'hobo'
    ports:
      - "7681:7681"
<% if (flagType === 'env' && goldnugget) { %>
    env_file: <%= uuid %>.env
<% } else if (flagType === 'file' && goldnugget) { %>
    volumes:
      - ./<%= uuid %>.gn:/goldnugget/<%= uuid %>.gn
<% } %>
